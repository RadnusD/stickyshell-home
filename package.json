{
  "name": "stickyshell-home",
  "version": "1.1.0",
  "description": "StickyShell Home - Lightweight Cross-Platform Sticky Note",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "start": "tauri dev",
    "dev": "tauri dev",
    "build:frontend": "node scripts/build-frontend.js",
    "build": "npm run build:frontend && tauri build",
    "test": "node test/test-core.js"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  },
  "dependencies": {
    "katex": "^0.18.7"
  }
}
