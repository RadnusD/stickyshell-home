name: Cross-Platform Release Build

on:
  push:
    branches:
      - main
      - master
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build-tauri:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: 'macos-latest'
            rust_targets: 'x86_64-apple-darwin,aarch64-apple-darwin'
            args: '--target universal-apple-darwin'
            bundle_path: 'src-tauri/target/universal-apple-darwin/release/bundle/'
          - platform: 'ubuntu-22.04'
            rust_targets: 'x86_64-unknown-linux-gnu'
            args: ''
            bundle_path: 'src-tauri/target/release/bundle/'
          - platform: 'windows-latest'
            rust_targets: 'x86_64-pc-windows-msvc'
            args: ''
            bundle_path: 'src-tauri/target/release/bundle/'

    runs-on: ${{ matrix.platform }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.rust_targets }}

      - name: Install Linux dependencies (Ubuntu only)
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libayatana-appindicator3-dev librsvg2-dev patchelf

      - name: Install Node dependencies
        run: npm ci

      - name: Build and Package with Tauri Action
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: v__VERSION__
          releaseName: 'StickyShell Home v__VERSION__'
          releaseBody: 'StickyShell Home automated release for Windows, Linux, and macOS.'
          releaseDraft: true
          prerelease: false
          args: ${{ matrix.args }}

      - name: Upload Build Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: stickyshell-${{ matrix.platform }}
          path: |
            ${{ matrix.bundle_path }}
            src-tauri/target/release/bundle/
          if-no-files-found: warn
